export let config = {
    "port": 3000,
    "allocated_threads": 6, //it's not recommended to exceed the number of cores on your machine. your machine cores * 2 is your anchor
    "hostname": "null"
}